files = [
  JASMINE,
  JASMINE_ADAPTER,
  'bootstrap.js',
  'index.js',
  'tests/*.js'
];

exclude = [];

// web server port
port = 9877;


// cli runner port
runnerPort = 9101;

colors = true;

logLevel = LOG_INFO;

singleRun = false;

captureTimeout = 60000;

browsers = ['PhantomJS'];